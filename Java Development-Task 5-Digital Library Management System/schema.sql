CREATE DATABASE IF NOT EXISTS library_db;
USE library_db;
-- Spring Boot JPA creates/updates the tables automatically with spring.jpa.hibernate.ddl-auto=update.
-- This script is intentionally minimal: it creates the database only; entity mappings are the source of truth.
